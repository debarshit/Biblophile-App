import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const ReadAlongsIndex = () => {
  return (
    <View>
      <Text>ReadAlongsIndex</Text>
    </View>
  )
};

export default ReadAlongsIndex;

const styles = StyleSheet.create({});